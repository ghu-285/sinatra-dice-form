<h1><%= @num %>d<%= @sides %></h1>

<a href="/process_roll?dice=<%= @num %>&sides=<%= @sides %>">Roll again</a> | <a href="/">Go back home</a>

<ol>
  <% @rolls.each do |a_roll| %>
    <li>
      <em>Result: </em><strong><%= a_roll %></strong>
    </li>
  <% end %>
</ol>
